export interface TimeColumnProps {
  title: string;
  field: string;
  sortable?: boolean;
  format?: string;
}

export function TimeColumn(
  //@ts-ignore
  props: TimeColumnProps
) {
  return null;
}
